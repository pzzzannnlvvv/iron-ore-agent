# 项目数据范围与训练测试划分

## 核心结论

本项目以 **45个港口进口铁矿石周度库存**（标的ID：ID00186052）为预测目标，数据采样频率为**周度**。训练集含 **470 行**（约9年），测试集含 **56 行**（约1年），合计约526周。测试集最后一个观测点为 **2026-05-01**，按周度频率推算测试集起始约为2025年4月中旬。**工具未返回训练集明确日期标签、训练集与测试集之间的 gap 设置、以及验证集划分方案**，这些属于当前工具结果中的未覆盖项。

---

## 一、主标的与数据来源

| 项目 | 信息 |
|------|------|
| 标的 ID | ID00186052 |
| 标的名称 | 铁矿石：进口：库存：45个港口（周度） |
| 数据口径 | 45个港口进口铁矿石库存 |
| 单位 | 万吨（根据 y_range 与 MAE 单位判断） |
| 日期列名 | `data_date` |

---

## 二、数据采样频率

数据采样频率为**周度**。依据测试集预测数据日期间隔为7天（如 2026-02-13 → 2026-02-20 → 2026-02-27），确认为周度采样。评估文件命名为 `final_evaluation_weekly.json` 和 `optimization_summary_weekly.json`，进一步印证频率为 weekly。

---

## 三、训练集与测试集划分

### 3.1 划分规模

| 划分项 | 行数 | 周数 | 约对应年数 |
|--------|------|------|------------|
| 训练集（train_rows） | 470 | 470 周 | 约 9.0 年 |
| 测试集（test_rows） | 56 | 56 周 | 约 1.1 年 |
| **合计** | **526** | **526 周** | **约 10.1 年** |

### 3.2 时间范围推算

由于工具未返回训练集的明确日期标签，以下时间范围基于行数和周度频率推算：

| 项目 | 日期 |
|------|------|
| 测试集最晚日期 | 2026-05-01（工具明确返回） |
| 测试集可见日期范围 | 2026-02-13 至 2026-05-01（工具返回最近12行） |
| 测试集推算起始日期 | 约 2025年4月中旬（约 2025-04-12 前后） |
| 训练集推算结束日期 | 约 2025年4月初（约 2025-04-05 前后，假设无 gap） |
| 训练集推算起始日期 | 约 2016年4月前后 |

> ⚠️ 以上训练集起止日期为基于行数和周度频率的**推算值**，工具未返回训练集的明确日期标签，实际日期可能存在偏差。

### 3.3 目标变量范围

| 项目 | 数值 |
|------|------|
| y_range（目标变量范围） | [13657.90, 17187.52]（万吨） |

---

## 四、训练集与测试集之间的 Gap（间隔）

**工具返回结果中未明确提及训练集与测试集之间是否存在时间间隔（gap）。**

根据训练集470行 + 测试集56行 = 526行总计，若数据连续无 gap，则总数据跨度约为526周（约10.1年）。但无法确认是否存在预留 gap 以防止数据泄露。

---

## 五、验证集划分

工具返回的评估文件和优化摘要中**仅包含训练集和测试集的指标**，**未提及独立的验证集**。

| 项目 | 状态 |
|------|------|
| 独立验证集 | 未提及 |
| 交叉验证策略 | 未说明（如 TimeSeriesSplit、expanding window 等） |
| Optuna 验证方式 | 优化中未说明验证策略（200次 trial） |

> 模型超参优化使用 Optuna 进行了 200 次 trial，最优 trial 的 MDA 为 86.05%，但优化过程中的验证策略未在返回结果中说明。

---

## 六、模型评估指标对比

| 指标 | 训练集 | 测试集 | 说明 |
|------|--------|--------|------|
| MDA（方向准确率） | 99.56% | 84.91% | 预测方向与实际方向一致的比例 |
| MAPE（平均绝对百分比误差） | 0.0263% | 2.87% | 预测值偏离实际值的百分比 |
| MAE（平均绝对误差） | 3.35 | 477.38 | 单位：万吨 |

**过拟合判断**：训练集 MDA（99.56%）与测试集 MDA（84.91%）差距 14.65%，存在一定过拟合。

---

## 七、模型优化配置

| 参数 | 值 |
|------|------|
| 模型 | LightGBM |
| 优化框架 | Optuna |
| 研究名称 | `lgbm_mda_optimization_weekly` |
| 试验总数 | 200（全部完成） |
| 耗时 | 1037.1 秒 |
| 随机种子 | 42 |
| 特征数量 | 3184（优化摘要）/ 3200（最终特征列表），存在轻微差异 |
| 最优 MDA（优化中） | 0.8605（86.05%） |
| 最优 MAPE（优化中） | 0.0122（1.22%） |

---

## 八、缺失或未覆盖信息汇总

以下关键时间序列配置参数在工具返回中**未包含**：

| 缺失信息项 | 状态 |
|------------|------|
| 训练集明确起止日期 | 未返回，上述日期为推算值 |
| 训练集与测试集之间的 gap | 未提及是否存在时间间隔及具体大小 |
| 预测步长（horizon） | 未返回 |
| 滚动预测窗口 | 未返回配置信息 |
| 时间序列交叉验证方式 | 未返回具体策略 |
| 验证集划分方案 | 未出现独立验证集信息 |
| 数据整体明确起止日期 | 无法获取精确起始日期（推算约为2016年4月） |
| 品种范围 | 未说明是否涉及分品种数据 |

以上信息可能存在于项目代码或配置文件中，但当前工具返回的数据仅包含最终评估结果、最优超参数和优化摘要，未涉及时间序列划分的细节配置。